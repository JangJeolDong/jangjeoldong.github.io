function korean() {
	if (w == 3) {
		ctx.fillStyle = "#000";
	} else {
		ctx.fillStyle = "#F90";
	}
	ctx.font = "48px GulimChe";
	ctx.textAlign = "center";
	ctx.textBaseline = "middle";
	ctx.fillText("한국인 창조 / CREATED BY KOREAN", 960, 1005);
}